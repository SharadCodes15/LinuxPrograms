[project]
name = "netspeed"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "SharadCodes15", email = "codebysharad@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "textual>=8.2.8",
]

[project.scripts]
netspeed = "netspeed:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
